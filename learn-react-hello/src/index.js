import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
    <App input="ABCD"/>,                // render what?  alsp data as PROPS
    document.getElementById('root')     // render where?
);
